using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerVision : MonoBehaviour
{
    /*[SerializeField] GameObject bloodyScreen1;
    [SerializeField] GameObject bloodyScreen2;
    public Animator animator;*/

    public GameObject bloodyScreen1;
    public GameObject bloodyScreen2;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*if (animator.GetInteger("Hit") == 0)
        {
            bloodyScreen1.gameObject.SetActive(false);
            bloodyScreen2.gameObject.SetActive(false);
        }
        if (animator.GetInteger("Hit") == 1)
        {
            bloodyScreen1.gameObject.SetActive(true);
            bloodyScreen2.gameObject.SetActive(false);
        }
        if (animator.GetInteger("Hit") == 2)
        {
            bloodyScreen1.gameObject.SetActive(false);
            bloodyScreen2.gameObject.SetActive(true);
        }*/
    }
}
